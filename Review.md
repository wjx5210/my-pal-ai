# My Pal AI 工程优化复盘

本文档持续记录每一轮优化的完整工程闭环。每次改动都应回答：为什么优化、依据是什么、采用什么方案、遇到了什么问题、如何验证，以及上线和回滚方式。

## 固定工作流

1. **发现问题**：用线上指标、测试结果或可复现现象说明问题，避免只凭感觉改动。
2. **确定目标**：给出本轮范围、成功标准和明确不做的事项。
3. **设计方案**：比较可选方案，记录收益、成本、风险与最终取舍。
4. **实施改动**：保持改动小而可审查，不夹带无关重构。
5. **自动化验证**：先运行默认离线回归；涉及模型质量时再运行在线 AI 评估。
6. **上线检查**：确认配置、密钥、数据、健康检查、监控和回滚条件。
7. **上线后观察**：检查错误率、延迟、资源和业务指标，再判断是否完成。
8. **复盘归档**：记录结果、遗留问题和下一步，不把经验只留在聊天记录里。

---

## 2026-08-31：建立可重复的测试质量基线

### 为什么要优化

后续计划会修改会话存储、缓存、监控和 RAG 检索。如果没有稳定且默认安全的回归入口，每次改动都可能引入旧功能退化，也可能误调用真实模型 API、产生费用，使开发者不敢频繁运行测试。

首次基线执行共收集 40 项测试。排除在线 AI 测试后，36 项通过、1 项在准备临时目录时失败。失败原因不是业务逻辑，而是 `pytest` 默认使用的系统临时目录在当前受限环境中不可写；默认缓存目录也出现权限警告。

### 优化目标

- 默认测试不访问真实 LLM 或 Embedding API。
- 测试临时文件仅写入项目内可控目录。
- 开发者用一条固定命令即可完成日常回归。
- 在线 AI 评估必须显式触发，避免意外费用。

### 方案与取舍

在 `pytest.ini` 中把默认标记设为 `not ai`，并把临时目录固定为 `.test-tmp`。同时关闭 pytest 缓存插件，因为缓存只提升少量本地体验，不值得在当前环境引入额外写权限依赖。

没有通过修改业务测试或跳过失败用例来获得“绿色结果”，因为问题来自测试运行环境，应该修复测试入口本身。

### 遇到的问题与解决办法

**问题：** 系统临时目录 `C:\Users\Lenovo\AppData\Local\Temp\pytest-of-Lenovo` 无访问权限。

**解决：** 使用 `--basetemp=.test-tmp`，使临时文件位于项目工作区；将该目录加入 `.gitignore`，避免运行产物进入版本库。

**问题：** `.pytest_cache` 无法写入并产生警告。

**解决：** 默认使用 `-p no:cacheprovider` 禁用非必要缓存，并忽略历史缓存目录。

### 测试命令

- 默认离线回归：`python -m pytest -q`
- 在线 AI 评估：`python -m pytest -m ai -q`
- 显式运行全部：`python -m pytest -m "ai or not ai" -q`

### 验证结果

- 后端默认离线回归：37 项通过，3 项在线 AI 测试按设计排除。
- 前端 ESLint：通过。
- 前端生产构建：通过，Vite 成功生成生产资源。
- 仍有 1 条来自 FastAPI TestClient 依赖链的弃用警告；它不影响当前结果，但需要在依赖升级轮次中单独处理，不能用全局忽略警告掩盖。

### 上线与回滚

本轮只调整开发测试配置和文档，不改变生产镜像的运行逻辑，因此无需部署生产服务。代码合并前必须通过默认离线回归。

若测试入口在其他环境不兼容，可回滚 `pytest.ini` 的 `addopts`；业务代码和线上数据不受影响。

### 遗留问题与下一步

- 前端目前只有构建和 lint，没有组件或端到端测试。
- 在线 AI 评估集规模较小，暂时不能代表真实回答质量。
- 下一轮优先建立请求可观测性，为后续优化提供线上证据。

---

## 2026-08-31：修复陆地坐骑查询的硬约束泄漏

### 为什么要优化

用户问题同时包含“前期”和“陆地坐骑”时，原结构化检索只解析“前期”，没有解析“可骑乘”和“陆地”条件。因此普通前期帕鲁进入候选集，随后被放入 Prompt，并作为参考帕鲁展示。

根因不在语言模型，而在检索层只应用了部分用户约束。Prompt 对错误候选做再好的表达，也无法恢复已经丢失的检索条件。

### 问题分类

这是一次**硬约束泄漏**：候选集合没有满足用户明确要求的必要条件。

工程上需要区分：

- **硬约束**决定候选是否合格，例如阶段、陆地骑乘、工作类型。
- **软偏好**只负责在合格候选内排序，例如奔跑速度更快。

不能用排序加分代替硬过滤，否则不合格候选仍可能进入 Top K。

### 优化目标

- 识别“陆地坐骑”“地面坐骑”“陆地骑乘”等明确约束。
- 候选必须具有陆地骑乘伙伴技能，排除飞行、水上和不可骑乘帕鲁。
- “有哪些”这类不带“推荐”关键词的问法也必须走结构化检索。
- 混合检索返回的参考帕鲁与过滤后的候选保持一致。

### 方案与取舍

根据知识库中已经规范化的伙伴技能描述，把坐骑分类为 `ground`、`water`、`flying`。本轮只开放用户明确提出的陆地坐骑过滤，不提前扩展尚未测试的泛化问法。

过滤顺序为：

1. 解析阶段、工作类型和坐骑类型。
2. 对每个帕鲁执行全部硬条件过滤。
3. 仅在合格候选中计算排序分数。
4. 陆地坐骑使用奔跑速度作为小幅排序因素。
5. 将 `mount_type` 写入检索元数据，供测试和后续展示验证。

没有使用大模型判断坐骑类型，因为当前结构化数据足以确定该事实；规则判断更稳定、便宜，也更容易测试。

### 测试驱动过程

先增加失败测试，再修改实现。失败测试稳定复现了两个现象：

- 结构化检索返回大量不满足坐骑条件的前期帕鲁。
- 混合检索把这些候选继续作为参考来源。

测试使用“前期陆地坐骑有哪些？”而非必须包含“推荐”的句式，以覆盖真实自然问法。预期候选严格限定为当前知识库中同时满足条件的美露帕、草莽猪、紫霞鹿、祇岳鹿和猎狼。

### 遇到的问题与解决办法

**问题：** 原入口要求查询必须含“推荐”“哪个好”等关键词，否则即使识别到坐骑条件也会回退到向量检索。

**解决：** 明确坐骑类型本身就是强结构化筛选意图，不再依赖推荐关键词。

**问题：** “可骑乘”不能直接代表陆地坐骑，飞行和水上坐骑同样可骑乘。

**解决：** 依据三种规范化伙伴技能句式先分类，再严格匹配 `ground`，避免只搜索“可骑”造成类别混淆。

### 验证结果

- 新增结构化硬约束测试：通过。
- 新增混合检索参考来源测试：通过。
- 后端默认离线回归：39 项通过，3 项在线 AI 测试按设计排除。
- 原有基地推荐、工作类型检索和 API 测试均未退化。
- 前端 ESLint 与生产构建：通过。
- Docker Compose 配置解析：通过。
- 本地 API 生产镜像：构建通过。Docker 默认代理链路两次造成依赖下载哈希不一致，改用主机网络后从相同官方包索引完成校验和安装；没有关闭哈希校验，也没有更改依赖版本。
- 本地生产容器：使用新镜像重建成功，健康检查为 `healthy`。
- 容器内结构化冒烟测试：返回草莽猪、猎狼、祇岳鹿、紫霞鹿、美露帕，且 `mount_type` 均为 `ground`。
- 真实 `/api/ask` 冒烟测试：成功；Top 3 回答及参考来源仅包含草莽猪、猎狼和祇岳鹿，未出现不满足坐骑条件的帕鲁。

### 上线检查

本轮修改后端检索逻辑，需要重新构建并部署 API 容器。上线前执行：

1. `python -m pytest -q`
2. `docker compose build api`
3. 使用预发布或本地生产容器验证“前期陆地坐骑有哪些？”
4. 确认回答及参考帕鲁均只出现合格候选。
5. `docker compose up -d api`
6. 检查健康状态与部署日志，再进行一次公网冒烟测试。

本轮不修改知识数据和向量库，不需要重新生成 Embedding。

当前状态：**已完成代码、自动化测试、生产镜像、本地容器验证和公网发布。**

### 公网发布结果

- 发布提交：`934bcad`。
- 服务器仓库：从 `6dace30` 快进更新到 `934bcad`。
- 发布方式：仅执行 `sudo docker compose up -d --build api`，未重建无关的 Web 容器。
- 服务器本地修改的 `data/vector_store.json` 得到保留，本轮未覆盖或重建向量库。
- API 启动日志正常，公网 `https://mypalai.space/health` 返回 `200 ok`。
- 公网首页返回 HTTP 200。
- 公网真实问答“前期陆地坐骑有哪些？”成功，回答和来源仅包含草莽猪、猎狼、祇岳鹿，未出现违反硬条件的候选。

发布后独立检查发现首页响应尚未包含 `Strict-Transport-Security`。这不阻塞本次检索修复，但应纳入后续 HTTPS 安全头加固任务。

### 回滚方式

若上线后出现结构化检索异常，回滚到上一应用镜像并重启 API 容器即可。数据格式、会话数据和向量库均未迁移，因此没有数据回滚步骤。

### 遗留问题与下一步

- 当前坐骑类型依赖规范化中文伙伴技能描述；未来数据源改文案时需要数据校验保护。
- 尚未支持“飞行坐骑”“水上坐骑”和泛指“坐骑”的完整结构化查询。
- 尚未把解析出的所有约束以统一对象返回，条件继续增加后应引入查询约束模型。
- 上线后应记录约束命中情况和零候选率，为规则扩展提供证据。

---

## 2026-08-31：建立 HTTP 请求追踪与耗时日志

### 为什么要优化

此前线上只有 Uvicorn 的基础访问行和可选问答日志。当用户反馈“请求很慢”或“某次请求失败”时，无法把浏览器响应、反向代理请求和后端处理日志可靠对应起来，也没有统一的应用处理耗时字段。

在继续做缓存、限流和性能优化前，必须先建立可观测基线，否则无法用证据判断优化是否有效。

### 优化目标

- 每个 HTTP 响应都返回唯一的 `X-Request-ID`。
- 支持透传合法的上游请求 ID，便于跨层关联。
- 拒绝包含空格、控制字符或过长的请求 ID，避免日志注入。
- 每个请求统一记录方法、路径、状态码和应用处理耗时。
- 不在访问日志中记录问题正文、Prompt、密钥或回答内容。
- 前端跨域调用时能够读取 `X-Request-ID` 响应头。

### 方案与取舍

使用轻量 FastAPI HTTP 中间件处理请求追踪，不引入外部监控依赖。请求 ID允许字母、数字、点、下划线、冒号和连字符，长度最多 128；不合格值替换为 UUID。

日志采用可检索的 `key=value` 格式，例如：

```text
request_complete request_id=... method=GET path=/health status_code=200 duration_ms=0.76
```

本轮只建立原始信号，不立即接入 Prometheus、Grafana 或外部告警平台，避免在没有稳定字段前扩大基础设施范围。

### 测试驱动过程

先增加失败测试，确认原响应缺少 `X-Request-ID`。测试覆盖：

- 未提供请求 ID 时生成合法 UUID。
- 合法上游请求 ID原样返回并出现在访问日志。
- 含空格的非法请求 ID被替换，避免直接写入日志。

### 遇到的问题与解决办法

**问题：** 单元测试能捕获 `app.access` logger，但生产 Uvicorn 日志配置没有输出该 logger，导致“测试通过、容器无日志”。

**解决：** 使用 `uvicorn.error.app_access` 子命名空间，复用生产 Uvicorn 日志管道，同时保留可筛选的独立 logger 名称。通过容器日志再次验证，而不是依赖单元测试推断生产行为。

**问题：** Docker 默认代理第三次造成 pip 下载内容与官方索引哈希不一致。

**解决：** 保留哈希校验，改用 `docker build --network=host`。依赖层命中安全构建缓存后，只重建应用层。

**问题：** PowerShell 在 Docker 构建失败后继续执行后续命令，第一次冒烟测试实际访问了旧容器，形成假通过风险。

**解决：** 每个关键原生命令后检查 `$LASTEXITCODE`，失败立即退出；明确区分“构建成功”“容器已重建”和“新容器冒烟通过”三个条件。

### 验证结果

- 后端默认离线回归：41 项通过，3 项在线 AI 测试按设计排除。
- 本地生产镜像：主机网络构建成功。
- 本地 API 容器：新镜像重建后健康。
- 自定义请求 ID `local-observability-smoke` 在响应头中原样返回。
- 同一 ID出现在容器访问日志中，并包含 `GET`、`/health`、状态码 200 和耗时。

### 上线与回滚

本轮只修改 API 应用，需要在服务器拉取 `main` 后重建 `api` 服务。公网验证应使用唯一请求 ID访问 `/health`，再在 API 日志中检索同一 ID。

如中间件导致异常，可回滚到上一提交并重建 API；本轮没有数据或配置迁移。

### 遗留问题与下一步

- 尚未让外层 Nginx 在其访问日志中记录同一个请求 ID。
- 当前只有原始日志，尚无请求量、P95 延迟和错误率聚合。
- 健康检查也会产生访问日志，后续可根据实际日志量决定是否降级为 debug。
- 下一轮可增加 Prometheus 指标或先完善 Nginx 安全头与日志格式。
